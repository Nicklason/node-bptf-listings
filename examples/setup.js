// Require the module
var Listings = require('bptf-listings');

var options = {
    "token": "", // Your backpack.tf access token
    "key": "" // Your steam api key
};

// Initialize the constructor with the options, you are now ready to go :)
var listings = new Listings(options);